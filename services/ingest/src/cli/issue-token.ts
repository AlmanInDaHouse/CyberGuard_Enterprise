// SPEC-004 FR-015 — enrollment-token issuance CLI (`task ingest:issue-token
// --org <id>`). Scaffold placeholder: the full implementation (insert an
// `enrollment_tokens` row in state `issued` with a 15-min TTL and print the
// opaque token once) lands with the B5 SPEC-004 implementation.

process.stderr.write("cg-ingest issue-token: not implemented yet (SPEC-004 B5)\n");
process.exit(1);
