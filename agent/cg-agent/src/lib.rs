//! `cg-agent` library — public surface for the integration harness.
//!
//! Empty in the scaffold commit. The modules described by SPEC-001
//! (`config`, `envelope`, `transport`, `shutdown`, `errors`) and
//! re-exports needed by `tests/` are added when the implementation
//! lands.
