# API schemas

OpenAPI definitions for the HTTP surfaces exposed by CyberGuard.

Expected artifact: `openapi.yml` describing the BFF (Fastify) endpoints consumed by the dashboard and external clients.

Populated by SPEC-XXX-api-surface.
