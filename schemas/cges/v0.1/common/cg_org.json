{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cyberguard.io/schemas/cges/v0.1/common/cg_org.json",
  "title": "CGES — Organization identity object",
  "description": "Identifies the customer organization. Multi-tenancy is reserved; in CGES v0.1 the tenant is always 'default'. See ADR-0006 and Blueprint §17.1.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "org_id": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9_-]{2,63}$",
      "description": "Stable organization identifier (slug)."
    },
    "tenant": {
      "type": "string",
      "const": "default",
      "description": "Reserved for future multi-tenancy. Fixed to 'default' in CGES v0.1."
    }
  },
  "required": ["org_id", "tenant"],
  "examples": [
    {
      "org_id": "acme",
      "tenant": "default"
    }
  ]
}
