# CGES v0.1 — examples

Validatable example payloads, one or more per category (process, network, auth, file, dns).

Each example must pass `ajv` against its corresponding schema. Examples double as fixtures for the harness.

Populated by SPEC-XXX-cges-v0.1.
