# Helm charts

Placeholder for Kubernetes deployment via Helm.

Out of scope for the MVP per the project non-goals. This folder exists as a structural placeholder so the layout does not change when Kubernetes support lands.
